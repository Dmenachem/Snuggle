import SwiftUI

struct HelpSupportView: View {
    var body: some View {
        List {
            Section {
                Text("Help & Support content will be implemented here")
                    .foregroundStyle(.secondary)
            }
        }
        .navigationTitle("profile.help.support".localized)
    }
} 